[package]
name = "simplan"
version = "0.1.0"
edition = "2024"

[dependencies]
avian3d = "0.2"
bevy = { version = "0.15", features = ["dynamic_linking"] }
bevy_image = "0.15"
fdm = { path = "../fdm" }
